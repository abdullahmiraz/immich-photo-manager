# User data & runtime (paths in .env are relative to project root)
library/
dedup-data/
data/
postgres/
pgdata/
compose/

# Secrets
.env
.env.local

# Tooling
*.aider
.aider/
.aider/*
.aider.input.history

# Deprecated / junk (do not re-add)
install.cmd
config/
